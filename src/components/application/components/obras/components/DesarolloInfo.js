import React from 'react'
import { Descriptions, Badge, List, Divider, Avatar, Tag, Space, Row ,Col, Carousel  } from 'antd';

export const DesarolloInfo = ({obraInfo}) => {


function onChange(a, b, c) {
  console.log(a, b, c);
}

const contentStyle = {
  height: '160px',
  color: '#fff',
  lineHeight: '160px',
  textAlign: 'center',
  background: '#364d79',
};
  return (
    <>

  <Row gutter={16}>
     <Col lg={12} xs={24}>

    </Col>
     <Col xs={24} lg={12}>

    </Col>
  </Row>

    </>
  )
}
